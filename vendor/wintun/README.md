# vendor/wintun

Populated by `cargo xtask fetch-vendor`. Binaries are **not** committed
(see `.gitignore`); the licence text for Wintun **is** committed here and ships
with the installer.

`cargo xtask verify-vendor` fails the build if the licence text is missing.
